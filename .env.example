# Root env for docker-compose.yml — copy to .env and fill in.
# Only Cloudinary needs real values; everything else has safe dev defaults
# baked into docker-compose.yml.

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
